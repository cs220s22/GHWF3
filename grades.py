
def compute_hw_average(grades):
	"""
	Compute the average grade for a list of assignment grades.
	
	Return a float representing the average grade. The param is a list of average grades" 
	"""
    if len(grades) == 0:
        return 0
    return grades[0]
